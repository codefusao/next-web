Never uses `any`
Avoid repeat strings in conditions, use ENUMS instead.
When using `map`,`find`,`filter`, do not call the current item as "item, call it as the actual item name, which usually is the list name in singular.
